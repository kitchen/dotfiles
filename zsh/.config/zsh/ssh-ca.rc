function signmebro() {
	pubkeyfile=${1:-${HOME}/.ssh/id_ed25519.pub}
	certfile=$(dirname ${pubkeyfile})/$(basename ${pubkeyfile} .pub)-cert.pub
	cert=$(VAULT_ADDR="http://localhost:8200/" vault write -field signed_key sshca/sign/kitchen public_key=@${pubkeyfile})
	echo $cert > $certfile
}
