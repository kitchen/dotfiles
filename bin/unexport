#!/bin/bash

netapp=$1
vol=$2

shift
shift

for host in $@ ; do
	ip=$(host $host | awk '{print $4}')
	echo "unexporting $host and $ip from $netapp:$vol"
	rnetapp $netapp export remove $vol $host
	rnetapp $netapp export remove $vol $ip
done

